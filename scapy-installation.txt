# 1. Update the package list
sudo apt update

# 2. Install Python3 development packages
sudo apt install python3-dev

# 3. Install pip (if not already installed)
sudo apt install python3-pip

# 4. Install Scapy using pip
sudo pip3 install scapy
pip install dpkt


# 5. (Optional) Install additional dependencies for Scapy
#    - For working with network interfaces:
#      sudo pip3 install pynmap
#    - For working with Wireshark:
#      sudo pip3 install pyshark
